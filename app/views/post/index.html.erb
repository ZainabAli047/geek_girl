<p id="notice"><%= notice %></p>

<h1>Post</h1>

<table>
  <thead>
    <tr>
      <th>Heading</th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
      </tr>
    <% end %>
  </tbody>

</table>

<%= link_to 'New Post', new_post_path %>